// Verificarea mai multor condiții:

let a = 10;
let b = 20;

console.log(a > 5 && b < 30);
console.log(a > 15 && b < 30);

// Verificarea condiției cu variabile boolean:

let isAdult = true;
let hasID = false;

console.log(isAdult && hasID);

// Confirmă dacă o idee este adevărată pentru ambele variabile:

let temperatura = 25;
let vreme = "plăcut";

console.log(temperatura >= 20 && vreme === "plăcut");

// Condiție complexă:
// nu inteleg conditie complexa

// Verifică dacă două condiții sunt adevărate:

let var1 = 15;
let var2 = 30;
console.log(var1 > 10 && var2 < 40);

// Verifică dacă ambele variabile boolean sunt adevărate:

let areAshi = true;
let areBatoane = false;
console.log(areAshi && areBatoane);

// Într-un scenariu de login:

let username = "admin";
let parolaCorecta = true;

console.log(username === "admin" && parolaCorecta);

// Condiție cu valori numerice:

let temperaturaDoi = 22;
let umiditate = 50;

console.log(temperaturaDoi >= 20 && umiditate < 60);

// Verificare dacă ambele variabile sunt diferite de zero:

let x = 5;
let y = 10;
